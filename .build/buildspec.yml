version: 0.2

#env:
#  variables:

phases:
  install:
    commands:
      - CODEBUILD_WORKING_DIR=`pwd`
      - sudo apt install -y binutils avr-gcc avr-libc uisp flex byacc bison

#  pre_build:
#    commands:

#  build:
#    commands:

#  post_build:
#    commands:
